#include <stdio.h>

/**
 * main - prints its name
 * @argc: argc parameter
 * @argv: array
 * Return: 0 if success
 */

int main(int argc, char *argv[] __attribute__((unused)))
{
printf("%d\n", argc - 1);
return (0);
}
