#include <stdio.h>

/**
 * main - prints its name
 * @argc: argc parameter
 * @argv: array
 * Return: 0 if success
 */

int main(int argc __attribute__((unused)), char *argv[])
{
printf("%s\n", *argv);
return (0);
}
