#include "main.h"
#include <string.h>

/**
 * _strlen - returns length of string
 * @s: input string
 * Return: length of string
 */

int _strlen(char *s)
{
int u;

i = 0;
while (s[i] != '\0')
{
i++;
}
return (i);
}
